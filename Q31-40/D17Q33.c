// Q33: Write a program to check if a number is an Armstrong number.

#include <stdio.h>
#include <math.h>
int armstrong_num(){
    
}
int main() {
    int digits;
   if (result == number) {
    printf("The %d is an Armstrong number", number);
   } else {
    printf("The %d is not an Armstrong number", number);
   }
    

}
